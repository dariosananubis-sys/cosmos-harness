## Copiadas

| fichero | qué hace | nicho COSMOS | qué se limpió |
|---|---|---|---|
| `wp-ssh.sh` | wp-cli remoto por SSH con ControlMaster (evita baneos fail2ban) y ejecución de PHP segura vía `eval-file` (nunca `wp eval` inline). Consolida en un solo script genérico el patrón repetido en `hwp.sh`/`hwp-php.sh`/`hwp-cliente.sh`/`hwp-identity*.sh`. Config por variables de entorno o `--sitio <slug>` contra un índice JSON. | `web` | Reescrito desde cero a partir del patrón de los 6 `hwp-*.sh`: fuera dominio `<agencia>solutions.es`, rutas Plesk hardcodeadas, nombres de variable `HORUS_SSH_*` y nombres de cliente en comentarios (marina, arcodelsol, apofis, rafamancheno). Todo parametrizado por env vars/JSON genérico. |
| `wp_sql.py` | SQL remoto sobre WordPress con red de seguridad: si el host no tiene cliente `mariadb`, cae a PHP con `$wpdb`. Helpers `wp()`, `consulta()`, `ejecuta()`, `php()`, `prefijo()`. | `web` | Quitada la lógica de elegir entre dos scripts (HORUS vs índice cliente) — ahora usa solo `wp-ssh.sh --sitio`. Fuera comentarios con nombres de cliente (`<cliente>.com`, `torroxbeachrentals`). |
| `navegador_seguro.py` | Wrapper de seguridad sobre `agent-browser`: nunca reutiliza la sesión de otro trabajo, verifica que la URL y el ancho de viewport cargados son los pedidos antes de fiarse de una medición. | `web` (también útil en `pruebas`) | Nombres de cliente en docstring/comentarios (`canicas`, `manzano`) sustituidos por ejemplos genéricos. |
| `wp-mcp-sync.py` | Registra en Claude Code un servidor MCP de Elementor (plugin EMCP Tools) por cada sitio de un índice JSON, con checks de seguridad (solo HTTPS, detecta credenciales de plantilla sin rellenar). | `agentes` | Ya estaba limpio; solo se renombró `cliente_id` → `sitio_id` para no dar por hecho que el índice es de clientes de agencia. |
| `wp-mcp-stdio-bridge.sh` | Sirve por STDIO el MCP de Elementor de un WordPress cuyo PHP del vhost es demasiado viejo para el plugin (usa el PHP del CLI del servidor, detectando la versión disponible). | `agentes` | Reescrito desde `wp-mcp-horus.sh`: fuera dominio `<agencia>solutions.es`, credenciales `HORUS_SSH_*`, y comentarios con nombres de cliente (arcodelsol, apofis, rafamancheno, marina). Config 100% por variables de entorno. |
| `cookies_catalogo.py` | Catálogo de >60 patrones de cookies (WordPress, WooCommerce, Elementor, Google Analytics/Ads, Meta, TikTok, LinkedIn, YouTube, Vimeo, PayPal, Stripe...) → clasificación legal (propia/tercero, tipo, finalidad, plazo) en castellano, para generar textos LSSI/RGPD correctos. | `legal` | 3 comentarios con nombres de cliente reales (mitetekids, iconikapublicidad, ireneurbano) sustituidos por descripciones genéricas del caso. |
| `legales-bloque-cookies.py` | Mide en el HTML real qué servicios de terceros carga una web (huellas regex: GA, Meta, Maps, YouTube...), cruza con el catálogo de Complianz, y escribe/actualiza de forma idempotente el bloque legal "tipos de cookies, titularidad y plazo" que exige la LSSI — en editor clásico o en Elementor. | `legal` | Comentario con nombre de cliente (`descalcitos`) genericizado. Rutas `RAIZ/tools/...` aplanadas (el fichero ahora vive junto a sus dependencias). |
| `legales-bloque-rest.py` | Misma escritura del bloque LSSI pero por REST API (`wp-json`), para sitios sin acceso SSH — usa `app_password` de WordPress. | `legal` | Docstring con 3 dominios de cliente reales sustituido por texto genérico. |
| `legales-crear-paginas.py` | Genera el texto mínimo del artículo 10 LSSI (aviso legal) y la página de política de cookies cuando una web no las tiene, de forma idempotente. | `legal` | Nombre de cliente (`manzano`) en comentario sustituido. |
| `legales-texto.py` | Sustitución literal de texto en una página legal (editor clásico o Elementor), con transporte comprimido para que SSH no trunque el PHP. Para corregir NIF/domicilio/razón social mal escritos. | `legal` | Ejemplo de NIF sustituido por un placeholder claramente ficticio (12345678Z). |
| `legales-cookies-complianz.py` | Rellena por wp-cli la ficha de cada cookie dentro del plugin Complianz (finalidad, caducidad, titularidad, tipo de servicio) — corrige lo que el plugin deja como "Propósito pendiente de investigación". | `legal` | Comentario con nombre de cliente (`torroxbeachrentals`) genericizado; constante `HWP` muerta eliminada. |
| `legales-lssi.py` | Auditor de páginas legales publicadas contra la LSSI: comprueba aviso legal (denominación, NIF, domicilio, contacto) y política de cookies (tipos, titularidad, plazo) sobre la web ya en producción. | `legal` | 4 dominios de cliente reales en docstring/comentarios sustituidos por placeholders (`<dominio-cliente>`, `ejemplo.com`). |
| `legales-purga-servicios-complianz.py` | Deja en Complianz solo los servicios de terceros medidos como realmente cargados (evita sobre-declarar Meta/LinkedIn/YouTube heredados de un asistente o de un clonado). | `legal` | Comentario con nombre de cliente (`torroxbeachrentals`) genericizado. |
| `woocommerce-verificar-catalogo.py` | Verifica que todo el catálogo WooCommerce publicado esté completo: con precio, comprable, con imagen, con categoría propia, y variaciones correctas. | `web` | **Reescrito** desde `verificar-catalogo.py`: se quitó toda la orquestación específica (`horus.sh`, subida por SSH directo, wrapper `hwp-<slug>.sh`) y se dejó solo la lógica de negocio (el PHP verificador, que es 100% genérico) sobre `wp_sql.py`. |

## Descartadas

- `hwp.sh`, `hwp-php.sh`, `hwp-cliente.sh`, `hwp-identity.sh`, `hwp-identity-php.sh`, `hwp-marina.sh`: 5-6 copias casi idénticas del mismo patrón wp-cli-por-SSH con dominio `<agencia>solutions.es` y credenciales `HORUS_SSH_*` hardcodeadas (una literalmente lleva el nombre de un cliente, `marina`). Consolidadas en un único `wp-ssh.sh` genérico.
- `web-checkpoint.py`, `web-contrato.py`, `web-contrato-produccion.py`, `web-gate.py` (76 KB), `web-sin-codigo.py` (89 KB), `webs-estado.py`, `mockup-html.py`, `gate_render.py`: motor interno de QA/gate de la agencia. Atados a la convención propia `webs.<agencia>solutions.es/<slug>/wordpress/`, a `progress/gates/` y a IDs de comprobación (C01-C22) definidos en otros documentos internos; `web-contrato.py` y `mockup-html.py` además tienen dominios de cliente reales incrustados en el propio código (no solo en comentarios). No es separable sin reescribir el motor entero.
- `migra-customcss-nativo.py`: traductor de CSS a controles nativos de Elementor, pero depende de volcados en `progress/dominios-nocode-2026-08-05/` y del esquema MCP propio; docstring con nombres de cliente reales (Mancheno/Aguamarina). Lógica de mapeo no separable en el tiempo disponible.
- `generar-logos-popup.py`: generador de los iconos del selector de modelo del popup interno de Claude/ChatGPT de la propia agencia (lee `.icns` de apps instaladas, escribe en `.claude/assets/logos/`). Sin valor genérico fuera de ese contexto muy concreto.
- `verificar-catalogo.py` (original): sustituido por la versión reescrita `woocommerce-verificar-catalogo.py`, que conserva la lógica de negocio pero sin la orquestación `horus.sh`/wrapper por cliente.

## Barrido de contaminación (esta tanda)

```
grep -rniE "<agencia>|<agencia>|dario|@gmail|@<agencia>|[0-9]{8}[A-Z]|ghp_|sk-|BEGIN [A-Z ]*PRIVATE KEY|password *=|token *=" /Users/<usuario>/cosmos/cosecha/
```

Sobre los ficheros de este clúster: **sin aciertos reales**. Los únicos matches de todo `cosecha/`
(incluye ficheros de otros clústeres en paralelo) son variables genéricas (`token = base64...`,
`GITHUB_TOKEN=...`, `pageToken=...`) y los placeholders de NIF ficticio `12345678Z` en
`legales-crear-paginas.py`/`legales-texto.py` — ningún nombre real, dominio de cliente ni credencial.
