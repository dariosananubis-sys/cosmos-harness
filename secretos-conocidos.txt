# Inventario de hallazgos del escáner de secretos que YA estaban en el árbol.
#
# Existe por una razón concreta: sin él, `puente/gate.py` devuelve 1 sobre este
# mismo repositorio, y un gate que no se puede instalar es exactamente el fallo
# que COSMOS documentó al auditar otro harness (spec/GUARDARRAILES.md).
#
# Reglas:
#   - Una línea por hallazgo aceptado, SIN el número de línea: mover código no
#     convierte deuda vieja en deuda nueva.
#   - Todo hallazgo que NO esté aquí bloquea el commit. La lista solo se encoge.
#   - Los valores siguen ocultos; aquí solo se nombra el fichero, que no es
#     secreto: ya está en el árbol.
#
# Deuda abierta: hallazgo H18 de reviews/revision-adversarial-final.md (datos
# personales en un repo declarado «público-limpio»). Los cuatro ficheros de
# cosecha/ los limpia quien tenga ese boundary; esta lista se vacía entonces.
# Para comprobar el estado real sin el inventario:
#   python3 -m puente.secretos --todo --sin-conocidos

# cosecha/patch-claude-mem-hooks.sh
ruta#1fae9196a0f8: posible teléfono
# cosecha/enviar-correo-smtp.py
ruta#27c57a873808: posible correo electrónico
# research/HERRAMIENTAS-PROPIAS.md
ruta#581adcbe1e97: posible identificador fiscal asignado
# cosecha/legales-texto.py
ruta#71fbc2f5da92: posible identificador fiscal asignado
# cosecha/legales-crear-paginas.py
ruta#728af2878291: posible correo electrónico
ruta#728af2878291: posible teléfono
