# Hallazgos aceptados del escaner de secretos. Una linea por hallazgo, sin numero de linea.
# Solo entran valores que NO son credenciales: claves de ejemplo publicadas por el propio
# fabricante y patrones del detector. Nunca una credencial real "que ya esta rotada".
#
# El unico inventariado es la clave de ejemplo que AWS publica en su propia documentacion
# (la que empieza por AKIA y acaba en EXAMPLE). Vive en tests/test_guardarrailes.py, en la
# prueba que verifica que el guard TAPA un secreto aparecido en la salida de Grep: quitarla
# dejaria la prueba sin nada que tapar.
#
# Y no se escribe aqui su valor literal: el escaner leyo este mismo fichero y se denuncio a
# si mismo, que es justo lo que tiene que hacer.
ruta#25dacd7d542a:350: posible clave AWS
#
# 2026-09-02: el informe del tercer revisor (progress/revision-profunda-2026-09-02/INFORME.md)
# reproduce el hallazgo B11 con la MISMA clave de ejemplo de la documentacion de AWS (la del
# secreto que empieza wJalr y acaba EXAMPLEKEY). Es la reproduccion copiable que exige la rule
# del revisor; quitarla dejaria el informe sin evidencia. No es una credencial.
ruta#db0d46f684d1:550: posible clave secreta AWS
ruta#db0d46f684d1:550: posible secreto asignado
