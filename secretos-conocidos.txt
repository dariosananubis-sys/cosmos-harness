# Hallazgos aceptados del escaner de secretos. Una linea por hallazgo, sin numero de linea.
# Solo entran valores que NO son credenciales: claves de ejemplo publicadas por el propio
# fabricante y patrones del detector. Nunca una credencial real "que ya esta rotada".
#
# Desde el 2026-09-03 cada hallazgo lleva la HUELLA del valor (`[valor <12 hex del sha256>]`)
# y la excepcion se compara con ella: indulta ESE valor en ESE fichero, no el fichero entero
# para esa clase de patron. Antes indultaba (fichero x clase) para siempre, y dos credenciales
# nuevas y distintas atravesaron el gate con exit 0 (auditoria 360, F-01). Mover el mismo
# valor de linea sigue sin convertirlo en nuevo; cambiar el valor, si.
#
# La etiqueta de ruta es opaca a proposito (puente/etiquetas.py); el fichero se dice aqui en
# claro, en el comentario, para que una persona pueda auditar la lista sin escribir codigo.
#
# tests/test_guardarrailes.py — la clave de ejemplo que AWS publica en su documentacion (la
# que empieza por AKIA y acaba en EXAMPLE), en la prueba que verifica que el guard TAPA un
# secreto aparecido en la salida de Grep: quitarla dejaria la prueba sin nada que tapar.
ruta#25dacd7d542a: posible clave AWS [valor 1a5d44a2dca1]
#
# progress/revision-profunda-2026-09-02/INFORME.md — reproduce el hallazgo B11 con la MISMA
# clave secreta de ejemplo de la documentacion de AWS (empieza wJalr y acaba EXAMPLEKEY). Es la
# reproduccion copiable que exige la rule del revisor; quitarla dejaria el informe sin evidencia.
ruta#db0d46f684d1: posible clave secreta AWS [valor 783b4ea9174c]
ruta#db0d46f684d1: posible secreto asignado [valor 78314b11be2e]
